#!/bin/bash

#Run this script once, to install all the required dependencies to build the resume from latex to pdf
sudo apt update
sudo apt-get install -y texlive texlive-latex-extra texlive-fonts-recommended texlive-fonts-extra